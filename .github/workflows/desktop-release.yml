name: Desktop Release

on:
  workflow_dispatch:
    inputs:
      target_ref:
        description: Desktop source branch, tag, or commit SHA
        required: true
        default: app-main
      requested_version:
        description: Optional exact SemVer; blank auto-selects package version or the next free patch
        required: false
        default: ''
      channel:
        description: Release channel
        required: true
        type: choice
        options:
          - prerelease
          - stable
        default: prerelease
      publish:
        description: Publish after verification; false leaves a draft
        required: true
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  release:
    permissions:
      contents: write
    uses: PME26Elvis/llm-research-archive/.github/workflows/desktop-release-reusable.yml@app-main
    with:
      target_ref: ${{ inputs.target_ref }}
      requested_version: ${{ inputs.requested_version }}
      channel: ${{ inputs.channel }}
      publish: ${{ inputs.publish }}
