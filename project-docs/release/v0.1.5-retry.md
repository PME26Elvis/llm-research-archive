# v0.1.5 release retry

The first v0.1.5 release attempt completed repository verification and all four native package builds but stopped before publication when strict CycloneDX generation detected an invalid workspace esbuild topology.

The dependency topology was corrected and verified on `app-main`. This retained operations branch exists only to trigger the same reusable release pipeline again without weakening verification or reusing incomplete release state.
